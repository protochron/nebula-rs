//! Generated protobuf types for Nebula's handshake and lighthouse wire
//! messages, compiled from `proto/nebula.proto` — copied verbatim from
//! slackhq/nebula v1.10.3's own `nebula.proto` so the wire format matches
//! exactly. See `build.rs`.
#![allow(clippy::all)]

include!(concat!(env!("OUT_DIR"), "/nebula.rs"));
